# Observer Pattern

## Intent

Observer is a behavioral design pattern that lets you define a subscription mechanism to notify multiple objects about any events that happen to the object they've observing

## What problems does the observer pattern solve?

Use the Observer pattern when changes to the state of one object may require changing other objects.

Use the Observer pattern when some object in your app must observe others.
